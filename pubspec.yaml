name: secure_chat
description: Final Project for CENG675 Cryptography course.This project is based on a secure communication model using RSA,3DES_OFB.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  rsa_encrypt:
  convert:

  #firebase_core:
  firebase_database:


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/end_2_end.jpeg
